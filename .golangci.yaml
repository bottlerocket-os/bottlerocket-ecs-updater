linters:
  enable:
    - staticcheck
    - unconvert
    - goimports
    - revive
    - ineffassign
    - vet
    - unused
    - misspell
  disable:
    - errcheck

run:
  timeout: 3m
  skip-dirs:
    - stacks
